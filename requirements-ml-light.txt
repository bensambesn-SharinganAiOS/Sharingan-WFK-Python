# Sharingan OS - ML Dependencies (Lightweight)
# Optimized for: 4GB RAM, CPU-only execution

# Core ML
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Lightweight ML (ONNX Runtime - CPU optimized)
onnxruntime>=1.15.0
onnx>=1.14.0

# PyTorch (CPU-only, no CUDA) - Lightweight models
torch>=2.0.0
torchvision>=0.15.0

# Optional: Joblib for model persistence
joblib>=1.3.0

# Utilities
python-dateutil>=2.8.0

# Install: pip install -r requirements-ml-light.txt
